#!/bin/bash
set -euo pipefail
for attempt in {1..60}; do
  if ip -4 -o addr show dev wg0 2>/dev/null | awk '{print $4}' | cut -d/ -f1 | grep -Fxq -- "$1"; then
    exit 0
  fi
  sleep 1
done
echo 'WireGuard address not ready' >&2
exit 1
